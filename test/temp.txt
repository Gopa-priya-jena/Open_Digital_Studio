// BITS[i] = BITS[i] - 1ULL;
        //
        // if (!(~BITS[i]))
        // // if (BITS[i] != 0xFFFFFFFFFFFFFFFF)
        // {
        //     std::uint64_t temp = BITS[i] + 1ULL;
        //     BITS[i] = (BITS[i]) | temp;
        //     return;
        // }







   // BITS_8[i] = BITS_8[i] - 1;
        // if (!(~BITS_8[i]))
        //
        // // if (BITS_8[i] != 0xFF)
        // {
        //     std::uint64_t temp = BITS_8[i] + 1;
        //     BITS_8[i] = (BITS_8[i]) | temp;
        //     return;
        // }
